// explicit type
let variable: string='explicit variable';

// implicit
let implicit = "implicit variable";

// making mistkes
/*let name: string='Kamui';
name = 33;*/

let numType: number=22;
let bool: boolean=true;

console.log(`"variable" is a ${variable}. and "implicit" is a ${implicit}. the "number" var is the ${numType}, and everything is ${bool}`);
